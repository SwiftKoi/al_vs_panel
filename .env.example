# Numeric group ID of the host's docker group.
# Find it with: getent group docker
DOCKER_GID=956

# Public DNS name used by the production Caddy gateway.
# The DNS A/AAAA record must point to this Docker host.
DOMAIN=panel.example.com
